{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "hono": "^4.8.10",
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  }
}
